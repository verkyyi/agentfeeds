interface:
  display_name: "Agent Feeds"
  short_description: "Use continuously refreshed local stream context"
  default_prompt: "Use $agentfeeds to set up background refresh, show my stream brief, and list templates I can subscribe to."

policy:
  allow_implicit_invocation: true
